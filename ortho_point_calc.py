points = []

count = int(input('Anzahl der fehlenden Punkte: '))

for i in range(1, count + 1):
    name = input(f'Name des {i}. Punktes: ')
    y = float(input('Y-Koordinate: '))
    x = float(input('X-Koordinate: '))
    points.append(name), points.append(y), points.append(x)

print(points(float[y]))
